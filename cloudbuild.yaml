steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/asgard-livestream/$REPO_NAME:$BUILD_ID', '.']
  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/asgard-livestream/$REPO_NAME:$BUILD_ID']
  # Deploy container image to Cloud Run
  - name: "gcr.io/cloud-builders/gke-deploy"
    args:
    - run
    - --filename=k8s/development/inlive-website.yaml
    - --location=asia-southeast1
    - --cluster=inlive-dev
    - --output=output/inlive-website
    timeout: 900s
images: [
  'gcr.io/$PROJECT_ID/$REPO_NAME:$BUILD_ID',
]
